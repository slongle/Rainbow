修复Bug:
LNK2005:
    现象: 当一个头文件a.h中有一个非inline的函数foo, 如果a.h被多个文件include, 那么foo会被重复定义多次。
	解决: 
	      方案1: 将foo函数变为inline函数。
		  方案2: 在头文件a.h里只是生命foo函数, 在该头文件的cpp文件a.cpp里实现foo函数。
	      
Assert(Express)
	当Express为假(0)时, 执行assert, 打印错误信息。
	所以Express写为要求达到的条件即可。当条件不满足时, Express为假, 执行assert。